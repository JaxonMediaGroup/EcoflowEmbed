{
  "name": "chatbots-analytics",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix analytics/frontend",
    "build": "npm run build --prefix analytics/frontend",
    "preview": "npm run preview --prefix analytics/frontend",
    "install:frontend": "npm install --prefix analytics/frontend"
  }
}
